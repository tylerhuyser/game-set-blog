import React from 'react'

export default function Tags(props) {
  
  const { tags, postTags } = props

  console.log(tags)
  
  const TAGSJSX = tags && tags?.map((tag, index) => {

  })

  return (
    <>
    </>
  )
}